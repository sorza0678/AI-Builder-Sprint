# --- 공통 (A: 분석 API) ---
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
python-dotenv>=1.0
pytest>=8.0
# --- B: 서비스·DB ---
psycopg2-binary   # Postgres(Supabase) 드라이버 — app/db.py 가 raw 쿼리로 사용
ruff              # 린터 (dev)
